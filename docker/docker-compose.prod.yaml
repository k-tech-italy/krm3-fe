version: "3.8"

services:
  krm3_fe:
    build:
      context: ../   
      dockerfile: docker/Dockerfile  
      target: prod   
    ports:
      - "3000:3000" 
    environment:
      - NODE_ENV=production
      - KRM3_FE_API_BASE_URL=https://api.tuosito.com/api/v1/  
